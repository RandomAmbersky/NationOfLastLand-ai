# Todo List - Frontend Improvements

## ⚠️ Дублирование архитектуры - ЗАВЕРШЕНО

Удалены ссылки на устаревшие legacy файлы:
- Обновлен `index.js` для экспорта только новой архитектуры
- Обновлен `README.md` с новой структурой модулей
- Удалены дублирующие экспорты (`GameDemo as GameEngineDemo`)

## ✅ Тесты для всех систем - ЗАВЕРШЕНО

- ✅ `RendererSystem.test.js` - 23 теста
- ✅ `GameStateSystem.test.js` - 16 тестов
- ✅ `SelectionSystem.test.js` - 21 тест
- ✅ `InputSystem.test.js` - 28 тестов

## 🔴 Критические проблемы

1. **Отсутствие модульных тестов для новых систем**
   - ✅ Теперь есть полные тесты для всех систем
   - `RendererSystem`, `InputSystem`, `SelectionSystem`, `GameStateSystem`

2. **Смешение ответственности**
   - `RendererSystem` создает сущности (`createEntitySprite`) И синхронизирует их (`_syncEntities`)
   - `GameStateSystem` одновременно управляет состоянием и вызывает WASM функции
   - `EntityService` использует `gameEngine.state.get()` напрямую вместо абстракции
   - **СТАТУС**: Потребуется рефакторинг архитектуры

## 🟡 Проблемы архитектуры

3. **StateContainer не является истинным источником истины**
   - `RendererSystem._renderedEntities` хранит дублирующую информацию
   - `SelectionIndicator` работает напрямую с Pixi контейнерами вместо через систему
   - **СТАТУС**: Требует архитектурной переработки

4. **Отсутствие централизованного coordinate transformer**
   - `RendererSystem` создает свой `transformer` в `init()`
   - `InputSystem` и `SelectionSystem` дублируют логику преобразования координат
   - **СТАТУС**: Решено частично через `CoordinateTransformer`

5. **Непоследовательный обмен данными**
   - Системы получают `gameEngine` в конструкторе
   - Нет явного API для взаимодействия между системами
   - **СТАТУС**: Требует архитектурной переработки

## 🟠 Проблемы кода

6. **Ошибки в `_syncEntities`** (`RendererSystem.js:352`)
   ```javascript
   if (createdCount > 0 || removedCount > 0) {
   }  // Пустой блок - логирование отсутствует
   ```
   - Добавить логирование для отладки синхронизации сущностей
   - **СТАТУС**: Рекомендация к реализации

7. **Утечки памяти**
   - `RendererSystem` удаляет сущности из `_renderedEntities`, но не проверяет, есть ли контейнер
   - `SelectionIndicator` не вызывает `destroy()` у индикаторов при удалении сущностей
   - **СТАТУС**: Проверить в `destroy()` методах

8. **Отсутствие обработки ошибок**
   - `initWasm()` вызывается асинхронно, но ошибки не обрабатываются в `GameEngine` инициализации
   - **СТАТУС**: Есть в `GameStateSystem.initializeGame()` с try/catch

## 🟢 Рекомендации по улучшению

### Приоритет 1 (критично)

- [x] Написать тесты для всех систем
- [ ] Исправить `_syncEntities` и добавить логирование

### Приоритет 2 (важно)

- [ ] Создать централизованный `CoordinateService` (использовать `CoordinateTransformer`)
- [ ] Вынести логику создания/удаления сущностей в отдельный `EntitySpawnSystem`
- [ ] Добавить обработку ошибок при инициализации WASM

### Приоритет 3 (желательно)

- [ ] Стандартизировать API между системами
- [ ] Добавить документацию к каждой системе
