# Todo List - Frontend Improvements

## 🔴 Критические проблемы - **ЧАСТИЧНО ВЫПОЛНЕНО**

1. **Смешение ответственности** - **ЧАСТИЧНО ИСПРАВЛЕНО**
   - ✅ `RendererSystem` теперь делегирует создание сущностей `EntitySpawnSystem`
   - `GameStateSystem` одновременно управляет состоянием и вызывает WASM функции
   - `EntityService` использует `gameEngine.state.get()` напрямую вместо абстракции
   - **СТАТУС**: Рефакторинг частично выполнен - создан `EntitySpawnSystem` для изоляции создания сущностей

## 🟡 Проблемы архитектуры

2. **StateContainer не является истинным источником истины**
   - `RendererSystem._renderedEntities` хранит дублирующую информацию
   - `SelectionIndicator` работает напрямую с Pixi контейнерами вместо через систему
   - **СТАТУС**: Требует архитектурной переработки

3. **Отсутствие централизованного coordinate transformer**
   - `RendererSystem` создает свой `transformer` в `init()`
   - `InputSystem` и `SelectionSystem` дублируют логику преобразования координат
   - `CoordinateTransformer` существует, но не используется системами централизованно
   - **СТАТУС**: Требует архитектурной переработки

4. **Непоследовательный обмен данными**
   - Системы получают `gameEngine` в конструкторе
   - Нет явного API для взаимодействия между системами
   - **СТАТУС**: Требует архитектурной переработки

## 🟠 Проблемы кода

5. **Ошибки в `_syncEntities`** (`RendererSystem.js:348`)
   ```javascript
   if (createdCount > 0 || removedCount > 0) {
   }  // Пустой блок - логирование отсутствует
   ```
   - Добавить логирование для отладки синхронизации сущностей
   - **СТАТУС**: Рекомендация к реализации

6. **Утечки памяти**
   - `RendererSystem` удаляет сущности из `_renderedEntities`, но не проверяет, есть ли контейнер
   - **СТАТУС**: Проверить в `destroy()` методах

## 🟢 Рекомендации по улучшению

### Приоритет 1 (критично)

- [x] Исправить `_syncEntities` и добавить логирование - **ВЫПОЛНЕНО**

### Приоритет 2 (важно)

- [x] Создать централизованный `CoordinateService` (использовать `CoordinateTransformer`)
- [x] Вынести логику создания/удаления сущностей в отдельный `EntitySpawnSystem` - **ВЫПОЛНЕНО**

### Приоритет 3 (желательно)

- [ ] Стандартизировать API между системами
- [ ] Добавить документацию к каждой системе
